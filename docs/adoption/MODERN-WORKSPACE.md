# Adopt the modern workspace foundation

Declare stable panes, resolve safe preferences below authorization/capability, and create heavy secondary controls lazily. Present semantic contextual actions through the existing action command registry. Use native layout for ordinary split views; adopt Actipro tabs only when their mature tab behavior is needed. See the architecture documents for ownership boundaries.
