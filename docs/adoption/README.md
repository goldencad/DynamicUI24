# Adoption

Consumer applications reference only the DynamicUI24 modules they need and supply their own business behavior, data access, permissions, and branding.

The sample application demonstrates only that a cross-platform Avalonia consumer can host the framework assemblies. PayCalc24 is a future consumer/reference implementation and is not part of the framework dependency graph.
