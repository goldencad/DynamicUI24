# Adopt the composer

Use a native multiline input, keep drafts in application/runtime state, reuse Resource Chips for attachments, bound mention/action results, and route Submit through an existing semantic command. Persist drafts only under an explicit application policy suitable for their sensitivity.
