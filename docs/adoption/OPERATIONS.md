# Adopt operations

Keep work in an application/provider. Publish safe snapshots with increasing generations to one `OperationCoordinator`, expose Cancel/Retry only when truthful, adapt terminal snapshots into the existing Notification Coordinator, and reattach workspace views using `OperationId`.
