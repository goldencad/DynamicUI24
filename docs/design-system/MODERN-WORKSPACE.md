# Modern workspace design

Keep primary content dominant. Reveal secondary panes, activity, detail, and overflow progressively. Inline actions stay sparse; destructive actions use overflow/confirmation and essential actions are never hover-only. Use compact chips, concise actionable content states, indeterminate progress when no real fraction exists, native keyboard focus, safe accessible names, and existing theme/density/localization tokens.
