# Breadcrumb Design System

Place the optional breadcrumb above the workspace. Use compact text links separated by `>`; mark the final destination visually and semantically current. At narrow widths retain root/current, collapse middle ancestors to `…`, and use the standard flyout menu for overflow. Labels localize at runtime and must not force horizontal scrolling or reduce the workspace below its safe minimum.
