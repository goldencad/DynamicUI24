# Sheet tabs

Tabs show eligible visible sheets in display order, mark the active semantic identity, and move excess items into overflow. Activation always passes `SheetCode`. Hidden and unauthorized sheets are omitted; tab captions, positions and localized titles never become identity. Keyboard/accessibility labels use the P1-safe presentation projection.
