# Workspace Designer

`WORKSPACE` defines identity, localized labels, `TemplateCode`, order, semantic `IconKey`, activity, company scope, permission and lifecycle metadata.

The template selector is generated from `TemplateRegistry.GetRegisteredTemplates()`. It contains no hard-coded standard-template branch: the Demo's independently registered `CALENDAR` template appears automatically. Unknown codes remain visible as invalid metadata and produce a safe validation diagnostic.
