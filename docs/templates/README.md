# Template modules

The Setup, DataEntry, Report, HistoryDocument, Dashboard, and Signing projects are isolated module shells for future work defined by Specification v0.6.

Task 0 intentionally contains no template behavior. Each module currently exposes only an assembly marker so the module can be built and its dependency boundary can be tested.
