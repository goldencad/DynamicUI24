# Task 10C backlog

Non-blocking follow-ups intentionally outside the frozen scope:

- polished non-contiguous multi-range rendering and deterministic multi-range clipboard serialization;
- column-header and row-header cell-range selection;
- region-aware second-stage Ctrl/Cmd+A behavior;
- first-error viewport navigation for errors outside the current window;
- application-specific confirmation UI for very large operations;
- richer reference lookup conversion while retaining generic contracts.

File import/export, headers, fill handles/series, formula execution, grouping, pivoting, charts, layout persistence, collaboration, and business validation remain later-task concerns rather than 10C backlog implementation.
