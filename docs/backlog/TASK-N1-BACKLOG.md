# Task N1 Backlog

Non-blocking work deliberately excluded from N1:

- A richer shared flyout runtime may replace the minimal `INotificationMenuService` bridge when the separate Context/Flyout task lands.
- A shell-level transient overlay/animation manager may improve toast placement and timing without changing notification lifecycle.
- Platform accessibility certification and automated screen-reader coverage remain separate validation work.
