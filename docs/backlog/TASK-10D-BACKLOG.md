# Task 10D backlog

Ideas intentionally outside the frozen 10D scope:

- persisted user import profiles and permanent import history
- richer safe XML selectors and a streaming JSON reader for very large JSON arrays
- non-seekable atomic import via an application-supplied replay/spool service
- fixed-width export designer
- AI schema inference and fuzzy mapping
- OCR, PDF/image extraction, formula execution and report design
- cloud/SFTP/email ingestion, scheduling and ETL orchestration
- application-specific business formats, duplicate rules and validation
