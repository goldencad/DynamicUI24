# Task P1 Backlog

The following are intentionally outside P1:

- Selection/workspace-wide reveal UI beyond the field-scope contracts.
- Reliable supported public-API capture adapters where each platform can prove capability honestly.
- Reliable future Auto signals; vendor-specific remote-desktop/screen-share detection matrices are excluded.
- Context Panel runtime integration when that runtime exists.
- Search/Favorites/Pinned/Recent runtime integration when those runtimes exist.
- Export preview UI when that surface exists.
- Endpoint DLP, USB/print enforcement, MDM, EDR, CASB, SIEM, DRM, watermarking, document/OCR redaction, AI sensitivity/legal classification, audit database/dashboard, secure enclave/key management, server/database authorization redesign, and product/business-specific classification.

P1 does not introduce PayCalc24 or Odoo rules/dependencies.
