# Task S1 Backlog

Deferred beyond the frozen S1 scope:

- persistent search-query history;
- application/database full-text providers and indexing services;
- semantic, vector, fuzzy-ML, embedding, or AI search;
- cloud search, OCR, and document extraction;
- drag/drop pin ordering and richer palette filtering UI;
- contextual Help provider, Context/Inspector Panel, and Breadcrumb runtimes;
- provider health telemetry and persistent failure notifications;
- partial streaming batches (S1 resolves a deterministic provider batch).
